apiVersion: terramate.io/cli/v1
kind: BundleInstance
metadata:
  name: db-credentials
  uuid: a1b2c3d4-0001-4000-8000-000000000013
spec:
  source: /bundles/catalogue/tf-aws-secret/v1
  inputs:
    name: acme/database/main-credentials
    description: Primary database credentials for the API service
    secret_type: rds-credentials
    enable_rotation: true
    rotation_days: 30
    recovery_window_days: 30
    tags:
      project: api
      team: platform
